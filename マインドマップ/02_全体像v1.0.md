# 02_全体像 v1.0

この図は、プロジェクト全体を「役割の塊」で俯瞰するためのものです。
NotebookLM の理解用マップを参考にしつつ、GitHub 上で読めるように Markdown + Mermaid で再構成しています。

```mermaid
flowchart LR
    ROOT["Instagram第二の脳構築プロジェクト"]

    ROOT --> P
    ROOT --> A
    ROOT --> M
    ROOT --> I
    ROOT --> S
    ROOT --> T

    subgraph P["プロジェクト理念と目的"]
        P1["第二の脳の構築"]
        P2["人間とAIの共通知識基盤"]
        P3["非エンジニアによる長期運用"]
        P4["原本データの永久保存"]
        P5["AI半依存・特定ベンダー回避"]
    end

    subgraph A["システムアーキテクチャ"]
        A1["文書体系"]
        A2["データ構造"]
        A3["一方向パイプライン"]
        A4["レイヤー分離の原則"]

        A1 --> A1a["企画書：思想・目的"]
        A1 --> A1b["設計書：構造・役割"]
        A1 --> A1c["仕様書：実装手順"]

        A2 --> A2a["4層ユニバーサルスキーマ"]
        A2 --> A2b["YAMLフロントマター"]
        A2 --> A2c["Markdownベース"]
    end

    subgraph M["データ移行プロセス"]
        M1["個別移行モジュール"]
        M2["実データ監査"]

        M1 --> M1a["通常投稿（IGP）"]
        M1 --> M1b["リール（IGR）"]
        M1 --> M1c["ストーリー（IGS）"]
        M1 --> M1d["サルベージ（IGX）"]

        M2 --> M2a["JSONスキーマ差分抽出"]
        M2 --> M2b["物理メディアパス解決"]
        M2 --> M2c["重複排除の原則"]
    end

    subgraph I["統合・投入工程（IGC）"]
        I1["Synapse衝突の回避"]
        I2["SystemLogs一覧の再生成"]
        I3["Obsidian投入順序"]

        I3 --> I3a["1. SystemLogs一覧"]
        I3 --> I3b["2. Synapses<br/>Tags / Mentions / Locations"]
        I3 --> I3c["3. 各種Posts"]
    end

    subgraph S["知識構造（Synapse）"]
        S1["エンティティ分類"]
        S2["設計方針"]

        S1 --> S1a["ハッシュタグ（概念）"]
        S1 --> S1b["メンション（人物）"]
        S1 --> S1c["位置情報（場所）"]

        S2 --> S2a["自動揺らぎ判定の禁止"]
        S2 --> S2b["自動統合・分離の禁止"]
        S2 --> S2c["人間による意味付けの受け皿"]
        S2 --> S2d["作業による判断履歴保持"]
    end

    subgraph T["運用ツールとUI"]
        T1["Obsidianプラグイン"]
        T2["Pythonバックエンド"]
        T3["グラフビューによる可視化"]

        T1 --> T1a["ダッシュボード表示"]
        T1 --> T1b["SynapseカードUI"]
        T1 --> T1c["メタデータ同期機能"]

        T2 --> T2a["高速Markdownパース"]
        T2 --> T2b["確認用HTML生成"]
        T2 --> T2c["軽量APIサーバー"]
    end
```

## 読み方

- `プロジェクト理念と目的` は、なぜこのプロジェクトをやるのかです。
- `システムアーキテクチャ` は、文書・データ・レイヤーの設計思想です。
- `データ移行プロセス` は、IGP / IGR / IGS / IGX と監査処理の全体です。
- `統合・投入工程（IGC）` は、今まさに詰めている統合と投入順序の領域です。
- `知識構造（Synapse）` は、何をどういう意味でノート化するかのルールです。
- `運用ツールとUI` は、最終的に人が使う側の機能群です。
